--- @class markup
markup = {}

--- (client) Parses markup into a [MarkupObject](https://wiki.facepunch.com/gmod/MarkupObject). Currently, this only supports fonts and colors as demonstrated in the example. 
--- [https://wiki.facepunch.com/gmod/markup.Parse]
--- @param markup string @ The markup to be parsed.
--- @param maxWidth number @ The max width of the output
--- @return MarkupObject
function markup.Parse(markup, maxWidth) end

